[project]
name = "samsung-controller"
version = "0.1.0"
description = "Multiple Display Control client for Samsung VM55B-U panels"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["samsung_controller*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
